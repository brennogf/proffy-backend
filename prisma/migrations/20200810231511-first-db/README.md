# Migration `20200810231511-first-db`

This migration has been generated by ibuuusteer at 8/10/2020, 8:15:11 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."users" (
"id" SERIAL,
"name" text  NOT NULL ,
"avatar" text  NOT NULL ,
"whatsapp" text  NOT NULL ,
"bio" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."classes" (
"id" SERIAL,
"subject" text  NOT NULL ,
"cost" integer  NOT NULL ,
"usersId" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."connections" (
"id" SERIAL,
"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"usersId" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."classSchedule" (
"id" SERIAL,
"week_day" integer  NOT NULL ,
"from" integer  NOT NULL ,
"to" integer  NOT NULL ,
"classesId" integer  NOT NULL ,
PRIMARY KEY ("id"))

ALTER TABLE "public"."classes" ADD FOREIGN KEY ("usersId")REFERENCES "public"."users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."connections" ADD FOREIGN KEY ("usersId")REFERENCES "public"."users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."classSchedule" ADD FOREIGN KEY ("classesId")REFERENCES "public"."classes"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200810231511-first-db
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+datasource postgresql {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider        = "prisma-client-js"
+  previewFeatures = ["transactionApi"]
+}
+
+model users {
+  id          Int           @id @default(autoincrement())
+  name        String
+  avatar      String
+  whatsapp    String
+  bio         String
+  classes     classes[]
+  connections connections[]
+}
+
+model classes {
+  id             Int             @id @default(autoincrement())
+  subject        String
+  cost           Int
+  user           users           @relation(fields: [usersId], references: [id])
+  class_schedule classSchedule[]
+  usersId        Int
+}
+
+model connections {
+  id         Int      @id @default(autoincrement())
+  user       users    @relation(fields: [usersId], references: [id])
+  created_at DateTime @default(now())
+  usersId    Int
+}
+
+model classSchedule {
+  id        Int     @id @default(autoincrement())
+  week_day  Int
+  from      Int
+  to        Int
+  classe    classes @relation(fields: [classesId], references: [id])
+  classesId Int
+}
```


